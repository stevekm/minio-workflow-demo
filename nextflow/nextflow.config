// settings to use local MinIO server for s3:// URL's
aws {
  // accessKey = 'minioadmin'
  // secretKey = 'minioadmin'
  accessKey = 'user1'
  secretKey = 'password1234'
  client {
     endpoint = "127.0.0.1:9010"
     protocol = 'http'
  }
}
